in progress:

done:
* Added simulation settings to FE
* Bug: Check why animals prefer to go to the left.

to do:
* Refactor program according to AI suggestions to spread the code currently in map.rs
* Only record every x generation (and a list of generations to start out with).
* Save each simulation in a dict
* Get list of generations and their status/settings
* Patch settings for a existing generation
* Continue running existing generation
* Change /simulation endpoint to start a generation (run in background).
* Add endpoint to get list of simulations
* Add endpoint to get status of one generation
* Add endpoint to patch settings of generation
* Add endpoint to continue running existing generation
* Update FE to use the new integrations


v 1.01
* Refactor animal and map vectors
* Add energy to entities, they use it to take action and die when it runs out
* Add conditions when generation is over
* Create new entities from the neurons of the old ones
* Add simulation run conditions that run simulation until fulfilled
* Add "record simulation" that records entities at start, and then a list of coordinates for each animal
* Add rest api to start simulation
* Add rest api to get simulation record
* Create vibe coded web project that lets you start simulation, get the result, and display it

v 1.0
* Entities can move and eat